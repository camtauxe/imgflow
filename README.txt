Instructions (assuming you are in the same directory as pom.xml)

This project is built using Maven, please make sure it is installed first

To build:

    mvn package

To run:

    java -jar target/imgflow-1.0-jar-with-dependencies.jar

To clean:

    mvn clean
